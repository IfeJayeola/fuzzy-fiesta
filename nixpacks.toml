[phases.setup]
nixPkgs = ["python311", "gcc", "pkg-config", "mysql", "freetype", "zlib", "libjpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = [
    "python manage.py collectstatic --noinput",
    "python manage.py migrate"
]

[start]
cmd = "gunicorn countries_project.wsgi
